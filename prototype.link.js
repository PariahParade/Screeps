/**
 * @returns {boolean} True, if this room is friendly, i.e. has a controller that is
 * owned by me.
 */
StructureLink.prototype.balanceEnergy = function () {
    if (this.energy === 0) {
        
    }
};